# Response headers for Cloudflare Pages (Vercel reads vercel.json instead).
# Same three headers either host, so the site behaves the same on both.
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN

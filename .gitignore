# Client personal data, and the DTR database, scan photos and signing key.
# Never commit. See prompts/operator-notes.md and specs/dtr-spec.md.
data/
out/

# Real prices and payment details, and DTR secrets.
config/services.local.toml
config/dtr.local.toml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Build output — deploy web/ directly, or rebuild with scripts/build-site.py
web/dist/

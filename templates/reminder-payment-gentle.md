Hi ${first_name}, just floating this back up — the invoice for ${service_name}
(${amount_due}) went out on ${invoice_date}. No rush if it's already on its way;
if anything about it needs adjusting, tell me and I'll sort it out.
